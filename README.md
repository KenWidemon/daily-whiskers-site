# Daily Whiskers Public Site

Static privacy and support pages. Operated by Kenneth Widemon.

Publish `main` / root with GitHub Pages. Public routes: `/privacy/` and `/support/`.
No build step, dependencies, JavaScript, analytics, or external font requests.
Only public website files belong here. Do not add app source, internal QA notes,
Firebase configuration, credentials, or user data.
